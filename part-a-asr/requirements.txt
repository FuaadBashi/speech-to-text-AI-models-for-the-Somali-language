# Core runtime dependencies for Part A (Python 3.9+)
# System dependency: ffmpeg + ffprobe must be installed separately (not via pip).

# Hugging Face stack
datasets>=2.20.0,<4.0.0
transformers>=4.45.0,<5.0.0
accelerate>=0.33.0,<1.0.0

# PyTorch (CPU/GPU install depends on your platform; keep minimal constraint)
torch>=2.0.0

# Numerics + utilities
numpy>=1.23.0
tqdm>=4.66.0
regex>=2024.0.0

# Audio I/O (used for WAV read/write in parts of the pipeline)
soundfile>=0.12.1

# Metrics
jiwer>=3.0.0

# Common HF/runtime extras (small, improves compatibility)
safetensors>=0.4.0
huggingface-hub>=0.23.0
packaging>=23.0

rapidfuzz>=3.9.7

